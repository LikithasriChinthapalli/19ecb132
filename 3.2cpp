#include<iostream>
#include<conio.h>
using namespace std;

int main()
{
	int *arr;
	int size;
	
	cout<<"enter the size of the input array:";
	cin>>size;
	cout<<"creating an array of size "<<size<<". .";
	arr =new int[size];
	cout<<"\ndynamic allocation of memory for array is succesful.";
	
	delete arr;
	getch();
	
}

output:

enter the size of the input array:5
creating an array of size 5. .
dynamic allocation of memory for array is succesful.
